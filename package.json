{
  "name": "bs-slash-create",
  "version": "21.0.0-pre.0",
  "description": "ReScript / BuckleScript bindings to the slash-create library for Discord commands",
  "main": "index.js",
  "scripts": {
    "build": "bsb -make-world",
    "build:docs": "bsdoc build SlashCreate && bsdoc support-files && prettier --write 'docs/**/*.html'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "slash-create": "=2.1.2"
  },
  "devDependencies": {
    "bs-platform": "^9.0.1",
    "bsdoc": "^6.0.4-alpha",
    "prettier": "^2.2.1",
    "slash-create": "=2.1.2"
  }
}
