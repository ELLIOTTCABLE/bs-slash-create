{
  "name": "bs-slash-create",
  "version": "2.1.0",
  "description": "ReScript / BuckleScript bindings to the slash-create library for Discord commands",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bs-platform": "^9.0.1"
  }
}
